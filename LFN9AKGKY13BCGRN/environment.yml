name: sf_env
channels:
  - snowflake
dependencies:
  # Streamlit in Snowflake supports Python 3.9, 3.10, and 3.11 for
  # warehouse runtimes; 3.11 is the default for newly created apps.
  - python=3.11
  - streamlit
  - pandas
  - snowflake-snowpark-python
