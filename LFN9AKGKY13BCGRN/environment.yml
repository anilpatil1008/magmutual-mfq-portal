name: sf_env
channels:
  - snowflake
dependencies:
  # Snowflake Streamlit package availability currently skips Python 3.11 for
  # pandas and snowflake-snowpark-python; use a supported runtime.
  - python=3.12
  - streamlit
  - pandas
  - snowflake-snowpark-python
